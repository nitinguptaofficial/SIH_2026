# Submission Package

The runnable submission is documented in the repository root [README.md](../README.md).

The application entry point is `src/app.py`; the trained model and source data are under `assets/`.
